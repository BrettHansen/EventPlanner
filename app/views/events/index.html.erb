<%= provide(:title, 'EventPlanner') %>

<h1>Featured Events</h1>
<%= render partial: 'common/preview_events', collection: @events %>

<% if current_user.admin? %>
    <p><%= link_to "Add a new event", new_event_path %></p>
<% end %>